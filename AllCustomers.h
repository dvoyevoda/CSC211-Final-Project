#pragma once
#include <string>

class AllCustomers {
    public:
        
    private:
        std::string firstName, lastName, streetAddress, city, state;
        int accountNumber, zipCode, phoneNumber;
};